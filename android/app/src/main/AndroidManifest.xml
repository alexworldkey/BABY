<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- ============================================================
         权限声明（Android 端）
         RECORD_AUDIO  ：【必配】幼儿跟读语音识别需要麦克风。
                         speech_to_text 插件在 Android 上依赖此权限，
                         首次调用 initialize() 时会自动弹出系统授权框。
         INTERNET      ：部分安卓设备的系统语音识别引擎需要联网
                         （离线引擎如个别厂商 ROM 可豁免，保留无副作用）。
         ============================================================ -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:label="水果英语"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher"
        android:usesCleartextTraffic="false">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <meta-data
                android:name="io.flutter.embedding.android.NormalTheme"
                android:resource="@style/NormalTheme" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 语音识别声明：让系统知道自己会用到 SpeechRecognizer -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>

    <!-- 声明需要使用系统语音识别服务（API 30+ 的包可见性要求，
         否则部分设备上 speech_to_text 可能找不到识别引擎） -->
    <queries>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>
</manifest>
